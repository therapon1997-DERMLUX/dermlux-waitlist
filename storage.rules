rules_version = '2';

// ΚΑΝΟΝΕΣ FIREBASE STORAGE
// Επικολλούνται ΜΟΝΟ στο: Firebase Console → Storage → Rules
// (ΠΟΤΕ στο Firestore Database → Rules!)

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
